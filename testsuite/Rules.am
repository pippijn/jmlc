include jml/Tests.am

TEST_EXTENSIONS = .jml

JML_LOG_COMPILER = $(srcdir)/testsuite/jml/runtest $(builddir)/jmlc$(EXEEXT)

$(srcdir)/testsuite/jml/Tests.am: $(wildcard $(srcdir)/testsuite/jml/*.jml)
	@echo -n 'TESTS +=' > $@
	$(AM_V_GEN)for i in $^; do echo -n " $$i" | sed -e 's|$(srcdir)/||' >> $@; done
	@echo >> $@
